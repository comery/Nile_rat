#!/usr/bin/env python3
import sys
import os

if len(sys.argv) < 4:
    sys.exit("python3 {} <reverse|makeup> <var.tba> <*.agp> <*.>")

def recover1(var, mat.agp):
    scaf_map = {}
    with open(mat.agp, 'r') as fh:
        for i in fh:
            tmp = i.strip().split()
            

